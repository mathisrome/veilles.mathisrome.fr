pipeline {
    agent {
        label 'master'
    }
    stages {
        stage('Build') {
            node ("build"){
                les
            }
        }
        stage('Deliver') {
            steps {
                sh "ls"
            }
        }
    }
}
