pipeline {
    agent {
        label 'master'
    }
    stages {
        stage('Build') {
            node ("build"){
                ls
            }
        }
        stage('Deliver') {
            steps {
                sh "ls"
            }
        }
    }
}
