pipeline {
    agent {
        label 'master'
    }
    stages {
        stage('Build') {
            steps {
              node {
                ls
              }
            }
        }
        stage('Deliver') {
            steps {
                sh "ls"
            }
        }
    }
}
