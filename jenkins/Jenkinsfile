pipeline {
    agent {
        label 'master'
    }
    stages {
        node ("build"){
            ls
            stage('Build') {

            }
        }

        stage('Deliver') {
            steps {
                sh "ls"
            }
        }
    }
}
